# This file enables the REST channel for Rasa
# It must be located in the directory where Rasa is run (backend/rasa/)

rest:
  # empty is fine — just having the key enables the /webhooks/rest/webhook endpoint
